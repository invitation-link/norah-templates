"use client";
import { FormEvent, useState } from "react";
import { trackEvent } from "@/app/lib/analytics";
import styles from "../marketing-pages.module.css";
export default function BespokeForm(){const[loading,setLoading]=useState(false);const[sent,setSent]=useState(false);const[error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/bespoke-requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const body=await response.json();setLoading(false);if(!response.ok){setError(body.error||"Could not send your request");return}setSent(true);trackEvent("bespoke_lead_submitted",{occasion:String(form.get("occasion")||"")})}if(sent)return <div className={styles.success}><h2>We have your brief.</h2><p>Invite Link will review it and contact you using the details provided.</p></div>;return <form id="bespoke" className={styles.form} onSubmit={submit}><label>Your name<input name="name" required minLength={2}/></label><label>Email<input name="email" type="email" required/></label><label>WhatsApp or phone<input name="phone" type="tel" required minLength={8}/></label><label>Occasion<input name="occasion" required placeholder="Wedding, housewarming, birthday…"/></label><label>Event date<input name="eventDate" type="date"/></label><label>Tell us what should feel special<textarea name="brief" required minLength={20} placeholder="Rituals, story, visual mood and interactions you have in mind."/></label>{error&&<p role="alert">{error}</p>}<button type="submit" disabled={loading}>{loading?"Sending securely…":"Request a bespoke quote"}</button></form>}
